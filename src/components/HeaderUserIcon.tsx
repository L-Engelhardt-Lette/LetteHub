import * as React from "react";
import Avatar from "@mui/material/Avatar";

function HeaderUserIcon() {
  return (
    <Avatar>
      {/* Use a letter or MUI's built-in icons */}
      {/* E.g., <PersonIcon /> for a person icon */}U
    </Avatar>
  );
}

export default HeaderUserIcon;
